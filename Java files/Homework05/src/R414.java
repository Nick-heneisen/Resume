public class R414 {
    // 2 is an integer, 2.0 is a double, '2' is a string, "2" is also a string, and "2.0" is also a string.
    // Strings cannot be added the same as integers or doubles, and integers can't be added the same as doubles.
}
